#include <stdio.h>

int main() {

    int arr[] = {10, 20, 30, 40, 50};
    int n = sizeof(arr) / sizeof(arr[0]);
    int suma = 0;

    for (int i = 0; i < n; i++) {
        suma += arr[i];
    }

    printf("La suma es: %d\n", suma);

    return 0;
}
